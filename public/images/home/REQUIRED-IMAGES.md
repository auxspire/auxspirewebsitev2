# Homepage Image Requirements

| Filename    | Use               | Dimensions (suggested) |
|-------------|-------------------|------------------------|
| hero-bg.jpg | Hero background   | 1920x800               |

The homepage trust strip uses verified delivery metrics (not client logos). Add `hero-bg.jpg` to `public/images/home/` when a photography-backed hero is preferred; the site works with gradient-only backgrounds until then.
